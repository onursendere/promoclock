---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { i18n, type Locale } from "@/lib/i18n/config";
import { getDictionary } from "@/lib/i18n/dictionaries";
import { getDeals, getTools } from "@/lib/content";
import { TOOL_CATEGORIES, getDealStatus, isLive } from "@/lib/deals";
import { breadcrumbJsonLd } from "@/lib/jsonld";
import { absoluteUrl, localePath } from "@/lib/seo";
import { BUILD_TIME } from "@/lib/site";
import SectionHeading from "@/components/SectionHeading.astro";
import ToolCard from "@/components/ToolCard.astro";
import ToolFilters from "@/components/islands/ToolFilters";

export function getStaticPaths() {
  return i18n.locales.map((lang) => ({ params: { lang } }));
}

const lang = Astro.params.lang as Locale;
const dict = getDictionary(lang);
const { tools: t, categories, nav } = dict.hub;
const [tools, deals] = await Promise.all([getTools(), getDeals()]);

const liveCount = new Map<string, number>();
for (const deal of deals) {
  if (isLive(getDealStatus(deal, BUILD_TIME))) liveCount.set(deal.tool, (liveCount.get(deal.tool) ?? 0) + 1);
}

const groups = TOOL_CATEGORIES.map((category) => ({
  category,
  tools: tools.filter((tool) => tool.category === category),
})).filter((g) => g.tools.length > 0);

const jsonLd = [
  {
    "@context": "https://schema.org",
    "@type": "ItemList",
    name: t.title,
    numberOfItems: tools.length,
    itemListElement: tools.map((tool, i) => ({
      "@type": "ListItem",
      position: i + 1,
      name: tool.name,
      url: absoluteUrl(localePath(lang, `tools/${tool.slug}`)),
    })),
  },
  breadcrumbJsonLd([
    { name: "PromoClock", path: localePath(lang) },
    { name: nav.tools, path: localePath(lang, "tools") },
  ]),
];
---

<BaseLayout lang={lang} dict={dict} path="tools" title={t.metaTitle} description={t.metaDescription} jsonLd={jsonLd}>
  <div class="mx-auto max-w-6xl px-4 pt-12 pb-24">
    <SectionHeading as="h1" title={t.title} subtitle={t.subtitle} />
    <div class="mb-12">
      <ToolFilters
        client:load
        root="#tools-root"
        allLabel={t.filterAll}
        liveLabel={t.withDeals}
        searchPlaceholder={t.searchPlaceholder}
        emptyLabel={t.empty}
        categories={groups.map((g) => ({ value: g.category, label: categories[g.category], count: g.tools.length }))}
      />
    </div>
    <div id="tools-root" class="space-y-14">
      {
        groups.map((group) => (
          <section data-tool-section aria-labelledby={`cat-${group.category}`}>
            <h2 id={`cat-${group.category}`} class="mb-5 font-sans text-sm font-semibold tracking-widest text-muted-foreground uppercase">
              {categories[group.category]}
            </h2>
            <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
              {group.tools.map((tool) => (
                <ToolCard tool={tool} liveCount={liveCount.get(tool.slug) ?? 0} lang={lang} dict={dict} />
              ))}
            </div>
          </section>
        ))
      }
    </div>
  </div>
</BaseLayout>
